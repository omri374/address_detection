from setuptools import find_packages, setup

setup(
    name='address_detector',
    packages=find_packages(),
    version='0.1.0',
    description='Detecting street addresses in free text',
    author='',
    license='MIT',
)
